spring:
  datasource:
    url: jdbc:postgresql://[db_host]:5432/postgres
    username: [db_username]
    password: [db_password]
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
